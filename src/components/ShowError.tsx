import React from "react";

// could be a pretty error message with fun design
const ShowError = () => {
  return <div className=''>An error occured while fetching the data!</div>;
};

export default ShowError;
